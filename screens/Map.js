import MapView, { Marker } from "react-native-maps";

function Map() {
    const region = {
        latitude: ,
        longitude:,
        latitudeDelta:,
        longitudeDelta:,
    }
  return <MapView initialRegion={}></MapView>;
}

export default Map;
